import { createMuiTheme } from '@material-ui/core/styles';

export const theme = createMuiTheme({
    custom: {
      primary: "#138CB4",
      complementary: "#065069",
      triad: "#16B5B5",
      black: '#222222',
      white: '#ffffff',
      grey: '#282828'
    }
})